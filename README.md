# 📊 CSV to Graph Converter (Python)

This project reads data from a CSV file and generates a graph using **Python**, **Pandas**, and **Matplotlib**.  
It is useful for visualizing meter readings, energy consumption, sensor values, sales data or any time-series dataset.

---

## ✅ Features
- Load and process CSV file
- Plot clean and readable graphs
- Auto-labels axes from CSV headers
- Works for any two-column data (e.g., Date vs Value)

---

## 📁 Project Structure

# csv_convert_to_graph
